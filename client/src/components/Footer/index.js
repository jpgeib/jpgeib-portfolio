import React from "react";
import { Image, Grid, Header } from "semantic-ui-react";

import "./style.css";

const Footer = (props) => {
    return (
        <div id="footer">
            <Header as="h1">This is the footer.</Header>
        </div>
    );
};

export default Footer;